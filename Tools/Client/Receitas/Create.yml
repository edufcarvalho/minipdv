info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/receitas"
  body:
    type: json
    data: |-
      {
        "prescritorId": 1,
        "pacienteId": 1,
        "compradorId": 1,
        "dataReceita": null,
        "dataCadastro": null,
        "produtos": [
          { "produtoId": {{id}}, "lote": "{{lote}}", "quantidade": 1 }
        ]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
