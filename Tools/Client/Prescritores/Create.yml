info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/prescritores"
  body:
    type: json
    data: |-
      {
        "nome": "Cassius Cley",
        "conselho": "CRM",
        "numero": "123456789",
        "UF": "GO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
