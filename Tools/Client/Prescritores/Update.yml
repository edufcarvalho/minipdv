info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/prescritores/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "id": {{id}},
        "numero": "123456",
        "nome": "Dr. Atualizado",
        "conselho": "CRM",
        "uf": "SP"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
