info:
  name: GetAll
  type: http
  seq: 1

http:
  method: GET
  url: "{baseUrl}/api/prescritores"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
