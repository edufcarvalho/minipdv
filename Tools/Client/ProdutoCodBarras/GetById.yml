info:
  name: GetById
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/produtos/{{produtoId}}/codbarras/{{codBarra}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
