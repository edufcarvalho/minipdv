info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/produtos/{{produtoId}}/codbarras"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "codBarra": {{codBarra}},
        "produtoId": {{produtoId}}
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
