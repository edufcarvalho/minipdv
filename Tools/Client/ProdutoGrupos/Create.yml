info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/produtogrupos"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Novo Grupo",
        "ativo": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
