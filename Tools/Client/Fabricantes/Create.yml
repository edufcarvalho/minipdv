info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/fabricantes"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nomeFantasia": "Novo Fabricante",
        "razaoSocial": "Novo Fabricante Ltda",
        "cnpj": "00000000000100",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
