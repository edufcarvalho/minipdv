info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/fabricantes/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "id": {{id}},
        "nomeFantasia": "Fabricante Atualizado",
        "razaoSocial": "Fabricante Atualizado Ltda",
        "cnpj": "00000000000100",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
