info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/vendas"
  body:
    type: json
    data: |-
      {
        "vendedorId": 1004,
        "clienteId": 1,
        "produtos": [
          { "produtoId": {{id}}, "quantidade": 1 }
        ]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
