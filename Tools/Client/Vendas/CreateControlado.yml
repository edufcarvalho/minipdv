info:
  name: CreateControlado
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/vendas"
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
