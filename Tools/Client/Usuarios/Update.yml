info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/usuarios/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Usuario Atualizado",
        "login": "user.atualizado",
        "ativo": true,
        "tipoUsuario": "Usuario",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
