info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/usuarios"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Novo Usuario",
        "login": "novo.user",
        "password": "mudar123",
        "tipo": "Usuario"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
