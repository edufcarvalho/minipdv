info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/clientes"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Novo Cliente",
        "cpf": "123.456.789-00",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
