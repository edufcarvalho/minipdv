info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/clientes/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "id": {{id}},
        "nome": "Cliente Atualizado",
        "cpf": "123.456.789-00",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
