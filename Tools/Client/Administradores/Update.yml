info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/administradores/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Administrador Atualizado",
        "login": "admin.atualizado",
        "ativo": true,
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
