info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/administradores"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Novo Administrador",
        "login": "novo.admin",
        "password": "admin123",
        "tipo": "Administrador"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
