info:
  name: CreateControlado
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/produtos/controlado"
  auth: inherit
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
