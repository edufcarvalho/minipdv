info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/principiosativos/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "id": {{id}},
        "nome": "Principio Atualizado"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
