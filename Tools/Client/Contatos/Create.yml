info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/contatos"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "email": "novo@email.com",
        "telefone": "11999999999"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
