info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/farmaceuticos"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Novo Farmaceutico",
        "login": "novo.farma",
        "password": "mudar123",
        "crf": "00000-SP"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
