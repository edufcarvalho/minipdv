info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/farmaceuticos/{{id}}"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "nome": "Farmaceutico Atualizado",
        "login": "farma.atualizado",
        "ativo": true,
        "crf": "00000-SP",
        "contatoId": null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
