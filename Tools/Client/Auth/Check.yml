info:
  name: Check
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/check"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
